interface:
  display_name: Package Design Handoff
  short_description: Package final implementation-ready handoffs
  default_prompt: Use $package-design-handoff to package this accepted design project as a complete versioned implementation handoff ZIP. For interim review, use $package-design-checkpoint instead.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  allow_implicit_invocation: true
