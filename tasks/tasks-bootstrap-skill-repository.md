# Tasks: Bootstrap Skill Repository

> Generated from [PRD: Bootstrap Skill Repository](prd-bootstrap-skill-repository.md)

## Acceptance Criteria Traceability

| AC | Criterion | Tasks |
|---|---|---|
| AC-1 | Canonical first skill is complete | 1.0 |
| AC-2 | Collection policy and usage are documented | 1.0 |
| AC-3 | Canonical commands validate without third-party dependencies | 1.0 |
| AC-4 | Packaging behavior has executable regression coverage | 1.0 |

## Relevant Files

- `README.md` — Collection documentation and catalog.
- `.gitignore` — Excludes bytecode generated by validation.
- `Makefile` — Canonical command surface.
- `scripts/check_skills.py` — Collection structure and source validator.
- `tests/test_package_handoff.py` — End-to-end packaging regression tests.
- `skills/package-design-handoff/` — Canonical first skill copied from the validated installed version.

### Notes

- Run all validation with `make check`.
- The implementation uses only GNU Make and the Python standard library.

## Tasks

- [x] 1.0 Bootstrap the skill collection <!-- Serves: AC-1, AC-2, AC-3, AC-4 -->
  - [x] 1.1 Add the validation command surface and write packaging tests before copying the skill.
  - [x] 1.2 Confirm the checks fail because the canonical skill is absent.
  - [x] 1.3 Add the complete `package-design-handoff` skill.
  - [x] 1.4 Replace the placeholder README with collection documentation and support policy.
  - [x] 1.5 Run all checks and reconcile the diff against the PRD.
  - **Validates when:**
    - Pre-implementation `make check` fails specifically because `skills/` or `package-design-handoff` is absent.
    - `make help` exits zero and lists `help` and `check`.
    - Post-implementation `make check` exits zero and reports one validated skill and three passing behavioral tests.
    - `git diff --check` exits zero.

### Task 1.0 Preflight

- Validation plan written: yes
- Validation plan saved in artifact: yes
- Validation review mode recorded in session state: `auto-proceed`
- Make targets or equivalent command surface identified: yes
- Acceptance criteria served by this task listed: yes
- Relevant files re-read before modification: yes

## Current Status

Implementation and automated verification are complete; awaiting human review.

## Validation Results

| Check | Result | Evidence |
|---|---|---|
| Validation fails before implementation | PASS | `make check` exited nonzero with `skills/ is required and must contain at least one skill`. |
| Canonical command discovery | PASS | `make help` listed `help` and `check`. |
| Collection and behavior validation | PASS | `make check` validated one skill and ran three tests with zero failures. |
| Standard skill validation | PASS | `quick_validate.py` reported `Skill is valid!`. |
| Canonical skill parity | PASS | Recursive diff against the installed skill produced no differences. |
| Patch hygiene | PASS | `git diff --check` exited zero. |

## Phase-Gate Audit

**Result:** PASS

**Rules compliance:**

- The approved `skills/<skill-name>/` layout, no-manifest decision, validation-first sequence, and Makefile command surface are implemented.

**Listed done but not actually done:**

- None.

**Required but not done:**

- Repository visibility remains public; changing it was explicitly outside this PR's scope.

**Done differently than documented:**

- None.

**Documentation/state updates needed:**

- Remove the active session-state file after human verification, before merge.

**Verification performed:**

- Red-first failure, `make help`, `make check`, standard skill validation, exact source comparison, and `git diff --check` all completed successfully.

**Recommended next action:**

- Review the pull request, make the repository private if still intended, and remove the session-state file before merge.
